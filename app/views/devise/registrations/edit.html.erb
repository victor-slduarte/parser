<div class="container">
  <h2>Edit <%= resource_name.to_s.humanize %></h2>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>

      <%= f.input :email,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:address] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string) %>
      <%= f.input :first_name,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:address] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string) %>
      <%= f.input :last_name,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:address] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string) %>
      <%= f.input :address,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:info_geral] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string) %>
      <%= f.input :phone_number,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:address] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string) %>
      <%= f.input :car_model,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:address] == 'true' || params[:info_geral] == 'true' || params[:angels] == 'true' ? :hidden : :string) %>
      <%= f.input :car_plate,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:address] == 'true' || params[:info_geral] == 'true' || params[:angels] == 'true' ? :hidden : :string) %>
      <%= f.input :password,
                  hint: "leave it blank if you don't want to change it",
                  required: false,
                  as: (params[:address] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string),
                  input_html: { autocomplete: "new-password" } %>

      <%= f.input :password_confirmation,
                  required: false,
                  as: (params[:address] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string),
                  input_html: { autocomplete: "new-password" } %>

      <%= f.input :current_password,
                  hint: "we need your current password to confirm your changes",
                  required: true,
                  as: (params[:address] == 'true' || params[:car_info] == 'true' || params[:angels] == 'true' ? :hidden : :string),
                  input_html: { autocomplete: "current-password" } %>

    </div>

    <div class="form-actions">
      <%= f.button :submit, "Update" %>
    </div>
  <% end %>

  <h3>Cancel my account</h3>

  <p>Unhappy? <%= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

  <%= link_to "Back", :back %>

</div>


