<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4">
      <!-- Insert form here -->
      <div class="form-login">

        <h2>Bem-vindo ao Parser</h2>

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email, required: false, autofocus: true, placeholder: "ana.veras@gmail.com" %>
            <%= f.input :password, label: "Senha", required: false, placeholder: "••••••" %>
            <%= f.input :remember_me, label: "  Lembrar dados pra login", wrapper_html: { class: 'lembrar-dados' }, as: :boolean if devise_mapping.rememberable? %>
          </div>
          <div class="form-actions">
            <%= f.button :submit, "Entrar", class: "btn btn-primary" %>
          </div>
        <% end %>
        <a href="/users/auth/facebook" id="facebook-button" class="btn btn-block btn-social btn-facebook">
        <i id="fb-logo" class="fab fa-facebook"></i>Entrar com o Facebook
        </a>
        <%= render "devise/shared/links" %>
        <h3>Ao cadastrar-me, declaro que sou maior de idade e aceito as Políticas de privacidade e os Termos e condições do Parser</h3>
      </div>

    </div>
  </div>
</div>




